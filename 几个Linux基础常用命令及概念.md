# 一些概念
collapsed:: true
	- ## 家目录
	  collapsed:: true
		- 又叫做 __主目录__
		- __家目录__ 是Linux中一个重要概念,表示存放当前用户所有文件的文件夹,这个文件夹下的所有文件所有者都是 __这个用户__ 而不是 __root__
		- 路径为 __/home/<用户名>__
		- 类似于Windows中的 __C:\\Users\\Adminstartor__
		- 这个文件夹下面有诸如 __Downloads(下载) Desktop(桌面) Documents(文档)__ 等文件夹,还有各种软件的数据和配置文件
	- ## 根目录
	  collapsed:: true
		- 就是 `/` ,在Linux中,所有的文件夹和文件都在根目录下面,这与Windows中的 __"分盘"__ (C:\\ D:\\ ...)概念不同
	- ## 隐藏文件
	  collapsed:: true
		- 在Linux中,以 `.` 开头的文件夹和文件会被认为是隐藏文件,在大多数时候隐藏
	- ## 文件夹路径相关
	  collapsed:: true
		- 想必你刚才一定注意到了,Linux路径是`/`分隔的,而Windows是`\`
			- Windows那边是因为历史遗留问题才使用`\`作为分隔,但大部分程序里面都使用`/`作为分隔符,因为`\`在编程和很多地方都作为 __转义符__ 使用,所以你甚至能在Windows见到两种混用的情况
		- 在大部分情况下,Linux命令行中命令的各种路径相关参数留空默认路径为当前工作目录路径
		- `~`
			- 动态路径,表示当前用户 __家目录__ 的路径
		- `..`
			- 表示上一级文件夹,多用于相对路径
	- ## 文件/文件夹权限相关
	  collapsed:: true
		- Linux中文件权限一般以 `drwxrwxrwx` 的格式显示
		- 可以将其分为四段 __d rwx rwx rwx__
			- 第一段 __d__
				- 表示是否是文件夹
			- 第二段到第四段 __rwx__ 分别表示 __所有者__ __同组用户__ __其他用户__ 权限
				- __r__ 表示 __读__ (read)权限
					- 没有这个权限将无法读取文件的内容
				- __w__ 表示 __写__ (write)权限
					- 没有这个权限将无法对文件进行写入或者其他修改文件的操作
				- __x__ 表示 __可执行__ (executable)权限
					- 如果一个脚本/二进制文件没有可执行权限,那么你就无法将其作为程序运行
		- 如果没有对应权限或者不是文件夹则使用 __-__ 占位
		- 例如
			- drwxrwxrwx
				- 表示这个文件夹对于所有者和同组成员以及其他人都可以读写和执行
			- -rwxr-xr-x
				- 表示这个文件对于所有者可读可写可执行,但是对于同组成员和其他人都只能读取和执行
			- -rwx------
				- 表示这个文件只能由所有者读取修改和执行
		- 数字表示法
			- 权限在文件系统中实际上以三个八进制数保存
				- `r`为4
				- `w`为2
				- `x`为1
				- 没有权限为0
				- 所以`rwx`就是4+2+1=7
				- `r-x`就是4+0+1=5
	- ## 当不知道命令怎么用的时候怎么办?
	  collapsed:: true
		- 你可以直接执行`命令 --help`这对绝大部分命令都有用,会输出命令的用法和参数信息
- # `cd`
  id:: 694f82c0-360d-4dfd-be0e-1d6fb8d419e3
  collapsed:: true
	- ### 切换 __当前工作文件夹__ 到另一个文件夹
	- ### 用法
		- `cd [路径]` (`[]`为可选参数)
	- ### 小技巧
		- #### 当你无路径直接调用`cd`时,会回到当前用户的 __家目录__
		- #### `cd -`
			- 使用 `cd -` 时,会返回上一次cd之前的文件夹
			  collapsed:: true
				- 你每cd一次,都会将其记录到 __dir stack__ 中
				- __dir stack__ 不只能存一个路径,我们可以使用在 `-` 后面加上数字来在 __dir stack__ 中的文件夹切换
					- 第一次`cd /tmp`
					- 第二次`cd /`
					- 第三次`cd ~`
						- 此时 __dir stack__ 里面
							- `~` `/` `/tmp`
							- 0      1       2
					- 然后执行`cd -2` ,然后你就发现回到了第一次cd的路径 __/tmp__
						- 相当于回退到两次cd之前
						- 这个时候的 __dir stack__
							- `/tmp` `~` `/`
					- 然后继续`cd -1`
						- 此时 __dir stack__
							- `~` `/tmp` `/`
				- `cd -` 等价于`cd -1`
- # `ls`
  collapsed:: true
	- ### 列出当前工作目录下所有文件和文件夹
	- ### 用法
		- ### `ls [参数] [路径]`
		- 当未输入路径时,默认路径为当前工作目录
	- ### 常用参数
		- ### `-a`
			- 列出所有文件/文件夹，包括 __隐藏的__ 文件夹和文件
		- ### `-l`
			- 列出文件/文件夹的同时列出其详细属性
			- #+BEGIN_NOTE
			  权限 所有组 所有者 文件大小(单位Byte) 修改时间
			  #+END_NOTE
		- #+BEGIN_TIP
		  可以直接组合参数达到同时应用两种参数的效果: `-la`
		  对绝大多数命令有效
		  #+END_TIP
- # `mv`
  collapsed:: true
	- ### 移动或重命名文件/文件夹
	- ### 用法
		- `mv [参数] <源文件/文件夹路径> <目标路径>` (`<>`为必要参数)
		- 当目标路径是一个不存在的文件/文件夹时,`mv`将执行重命名操作
	- ### 示例
		- `mv /home/pcxlk/this-is-a-text-file.txt /tmp` (`/tmp`是Linux中存放临时文件的文件夹)
			- 将`/home/pcxlk`下的`this-is-a-text-file.txt`文件移动到`/tmp`里面
		- `mv tmp.txt README.md`
			- 将当前工作目录下的`tmp.txt`重命名为`README.md`
		- `mv tmp.txt /tmp/trash` (假设/tmp/trash不存在)
			- 将当前工作目录下的`tmp.txt`移动到`/tmp`并重命名为`trash`
- # `cp`
  collapsed:: true
	- ### 复制文件/文件夹
	- ### 用法
		- `cp [参数] <源文件/文件夹路径> <目标路径>`
	- ### 常用参数
		- `-r`
			- 递归复制文件/文件夹
			- #+BEGIN_IMPORTANT
			  当源里面 __包含文件夹__ 的时候,必须使用此参数
			  #+END_IMPORTANT
		- `-a`
			- 复制时保留源文件/文件夹的权限
			- 而不是将其覆盖成目标路径文件夹的权限
	- ### 示例
		- `cp file.txt /tmp`
			- 把 __当前工作目录__ 下的 `file.txt` 复制到 `/tmp` 里面
		- `cp -r Trashs /tmp`
			- 把 __当前工作目录__ 下的 `Trashs` 文件夹及其所有文件复制到 `/tmp` 里面
		- `cp -r /home/pcxlk /home/pcxlk/backup`
		- 把`/home/pcxlk`文件夹及其所有文件复制到`/home/pcxlk/backup`
- # `mkdir`
  collapsed:: true
	- ### 创建文件夹
	- ### 用法
		- `mkdir [参数] [文件夹路径]`
	- ### 常用参数
		- `-p`
			- 需要时创建目标目录的父目录
	- ### 示例
		- `mkdir newDir`
			- 在 __当前工作目录__ 创建一个名为`newDir`的文件夹
		- `mkdir Desktop/dir` (假设`Desktop`存在)
			- 在 __当前工作目录__ 下的`Desktop`文件夹里面创建一个名为`dir`的文件夹
		- `mkdir -p /tmp/otto/Rush` (假设`/tmp`下没有`otto`文件夹)
			- 先在`/tmp`里面创建`otto`文件夹,然后接着在`/tmp/otto`里面创建`Rush`文件夹
- # `chmod`
  collapsed:: true
	- ### 用于更改一个文件夹/文件的权限
	- ### 用法
		- `chmod [参数] <权限操作参数> <目标文件/文件夹>`
	- ### 常用参数
		- `-R`
			- 将权限操作应用到目标文件夹及其下所有文件/文件夹
	- ### 权限操作参数
		- 有两种表示方法
			- 常规方法
				- 由三部分组成
				  collapsed:: true
					- 目标权限组
					  collapsed:: true
						- `u` (user)
							- __所有者__ 的权限
						- `g` (Group)
							- __同组用户__ 的权限
						- `o` (Other)
							- __其他用户__ 的权限
						- `a` (All)
							- 等效于`ugo`
					- 操作
					  collapsed:: true
						- `+`
							- 加权限
						- `-`
							- 减权限
						- `=`
							- 设置权限
					- `rwx`权限
				- 例如
				  collapsed:: true
					- `u+x`
					  collapsed:: true
						- 给所有者加上可执行权限
					- `g-rw`
						- 给同组用户去掉读写权限
					- `o=rwx`
						- 给其他用户设置可读写可执行
			- 八进制数字法
			  collapsed:: true
				- 与概念处相同
				- 例如
					- `777`
						- __所有者 同组用户 其他用户__ 都可读写可执行 (4+2+1=7)
					- `445`
						- __所有者 同组用户__ 可读 (4+0+0=4)
						- __其他用户__ 可读可执行 (4+0+1=5)
- # Written By PTALTS-LK
	- ## CC BY-NC-SA 4.0